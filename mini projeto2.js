// Mini Projeto 2
// Escreva uma função que usa 2 números e retorna o maior entre eles:

let numeros = maior(723, 725);
console.log(numeros);
function maior(num1, num2) {
    if (num1 > num2)
        return num1;
    else return num2;
}